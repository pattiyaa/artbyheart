<%if  tags.size >0 %>
    <i class="fa fa-tags" aria-hidden="true"></i>
<%end%>
<% tags.each_with_index do |t,index| %>
    <span> <%= t.name%> </span>
    <%if  tags.size >1 and index < tags.size-1 %>
       <span>,</span>
    <%end%>
<% end %>