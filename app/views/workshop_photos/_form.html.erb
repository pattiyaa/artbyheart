<%= simple_form_for(@workshop_photo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :workshop %>
    <%= f.input :photo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
